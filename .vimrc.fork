" .vimrc.fork
" vimrc file for this fork of spf13-vim
" place vimrc directives here for all users of this fork
" these directives override the base vimrc and can be overridden
" by vimrc.local directives.

" remove folding
set nofoldenable

" Yank from the cursor through the end of the line (including CR/LF),
"   this is default and not consistent with C and D.
nnoremap Y yy

set listchars=tab:>-,trail:.,precedes:«,extends:»,nbsp:¬
